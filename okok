<!DOCTYPE html>
<html lang="en">
<head>

</head>
<body>
  <div class="category-container" *ngFor="let categoryName of getCategoryNames()">
    <h2 class="category-title">{{ categoryName }}</h2>
    <input
      type="text"
      class="search-bar"
      placeholder="Search post header..."
      [value]="searchTerms[categoryName]"
      (input)="updateSearch(categoryName, $event)"
    />
    <div class="post-row">
      <ng-container *ngFor="let post of filterPosts(groupedPosts[categoryName], searchTerms[categoryName])">
        <div class="post-card">
          <div class="post-header">
            <h3>{{ post.post_header }}</h3>
          </div>
          <div class="post-info">
            <p class="posted-by" *ngIf="loggedIn"><strong>Post ID:</strong> {{ post.post_id }}</p>
            <p class="posted-by"><strong>Posted by:</strong> {{ post.posted_by }}</p>
            <p class="post-time"><strong>Posted at:</strong> {{ post.posted_at | date: 'MMM d, y h:mm a' }}</p>
          </div>
          <div class="post-content" *ngIf="!loggedIn">
            <p>{{ post.post_content }}</p>
          </div>
          <div class="post-buttons">
            <button class="btn-1" (click)="togglestatus(post)" *ngIf="loggedIn">
              {{ post.status === 'ACTIVE' ? 'Deactivate' : 'Activate' }}
            </button>
            <button class="btn-1" (click)="editPost(post.post_id)" *ngIf="loggedIn">Edit</button>
            <button class="btn-1" (click)="viewPost(post.post_id)" *ngIf="loggedIn">View</button>
            <button class="btn-1-1" (click)="viewPost(post.post_id)" *ngIf="!loggedIn">View</button>
            <button class="btn-2" (click)="deletePost(post.post_id)" *ngIf="loggedIn">Delete</button>
          </div>
        </div>
      </ng-container>
    </div>
    <p *ngIf="filterPosts(groupedPosts[categoryName], searchTerms[categoryName]).length === 0" style="color: gray; padding-left: 10px;">
      No matching posts found.
    </p>
  </div>
  <div class="feature-icon" (click)="tableView()">
    <svg width="53" height="30" top="20" viewBox="0 0 24 24" fill="none"  xmlns="http://www.w3.org/2000/svg" >
      <rect x="3" y="3" width="8" height="8" rx="2" fill="white"/>
      <rect x="13" y="3" width="8" height="8" rx="2" fill="white"/>
      <rect x="3" y="13" width="8" height="8" rx="2" fill="white"/>
      <rect x="13" y="13" width="8" height="8" rx="2" fill="white"/>
    </svg>
  </div>
</body>
</html>

css


html, body {
  height: auto; 
  margin: 0;
  padding: 0;
  background-color: #ebebed7d; 
  
}

body {  
  background-image: url('https://www.renewallogistics.com/wp-content/uploads/bottom_overlay.png');
  background-size: 900px 800px;
  background-repeat: no-repeat;
  background-position: 650px -290px; 
  font-family: 'Poppins', sans-serif; 
}


.post-table {
  width: 100%;
  height: 100%;
  border-spacing: 15px;
  background: linear-gradient(135deg, #ffffff, #2999e342);
} 

.body{
  background-color: #ebebed; 
}


.post-row {

  display: flex;
  flex-wrap: wrap;
  justify-content: flex-start;
}

.post-card {
  width: 250px;
  background: #ffffff;
  border-radius: 10px;
  box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.2);
  overflow: hidden;
  border: 1px solid #ddd;
  padding: 10px;
  transition: transform 0.3s ease-in-out;
  margin: 20px;
  height: 230px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.post-header {
  
  background: #3a5ce9;
  color: rgb(255, 255, 255);
  text-align: center;
  font-size: 16px;
  font-weight: bold;
  border-radius: 10px 10px 0 0;
  height: 60px;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 8px;
}

.post-info {
  display: flex;
  flex-direction: column;
  background: #ffffff;
  box-shadow: 1px 1px 4px rgba(0, 0, 0, 0.1);
  padding: 5px;
  border-radius: 5px;
  font-size: 10px;
  margin: 5px;
  line-height: 1.2;
}

.posted-by, .post-status, .post-time {
  padding: 1px 0;
  margin: 0;
  text-align: left;
  color: #777;
  font-weight: normal;
  font-size: 10px; 
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}


.post-content {
  padding: 5px;
  font-size: 12px;
  color: #333;
  text-align: center;
  height: 180px; 
  overflow-y: scroll;
  scrollbar-width: none;
}

.post-content::-webkit-scrollbar {
  
  display: none;
}


.post-buttons {
  display: flex;
  justify-content: space-evenly;
  padding: 5px;
  border-top: 1px solid #ddd;
  background: #fff;
  position: relative;
  bottom: 0;
  width: 100%;
}


button {
  position: relative;
  top: 15%;
  background-color: #4cd5f7;
  color: rgb(20, 19, 19);
  padding: 4px 7px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-size: 12px;
  transition: background-color 0.3s ease;
}

.button:hover{
  background-color: #3a5ce9;
  color: rgb(255, 255, 255);
}


.btn-2 {
  background-color: #f50909bb;
}


.btn-1-1 {
  background-color: #4cd5f7;
  width: 95%;
  height: 108%;
}

.btn-2:hover {
  background-color: #c50606;
 
}


.category-title {
  font-size: 24px;
  margin: 0; 
  padding: 10px; 
  color: #2e3eec;
  background-color: #ebebed00; 
  border-radius: 5px; 
  width: 20%;
}


.search-bar {
  width: 250px;
  margin: 0 15px 20px;
  padding: 6px 10px;
  border: 1px solid #ccc;
  border-radius: 6px;
  font-size: 14px;
}


.feature-icon {
  position:absolute;
  top: 15% !important;
  right: 4px !important;
  cursor: pointer;
  border-radius: 10px;
  height: 5%;
  width: 4%;

}

body {  
  background-image: url('https://www.renewallogistics.com/wp-content/uploads/bottom_overlay.png');
  background-size: 900px 800px;
  background-repeat: no-repeat;
  background-position: 650px -290px; 
  font-family: 'Poppins', sans-serif; 
}

so move this image when scorlled from top right to middle then in bottom left in somooth animation effect when scrolled
